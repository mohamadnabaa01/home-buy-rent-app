import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'home-and-car-buy-rent-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
